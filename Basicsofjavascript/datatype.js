 /**js is dynamically typed langauge i.e in js we don't need to define datatype explicitly, like what kind of data we are assigning to the variablev, it will accordingly take the datatype. 
  * Primitive and Non-Primitive Datatypes
  * Num           Object
  * String        Array
  * Boolean       Function
  * Null          Date
  * Undefined     RegX
 */

let f=78;
console.log(typeof(f));

let status = null;
console.log(typeof(status));

let value = undefined;
console.log(typeof(value));