/**push method = Adding new elements such as some numbers and some string values to the end of the array using the push() method. */

const arr = [3,4,5,6,7,8];
 arr.push(1,20);
 console.log(arr);

 /**pop method */
 // Declare and initialize array
let numArr = [20, 30, 40, 50];

// Removing elements from end 
// of an array
numArr.pop();

console.log(numArr);


// Declare and initialize array
let strArr = ["amit", "sumit", "anil"];

// Removing elements from end 
// of an array
strArr.pop();

console.log(strArr);
